<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema principal de Sponsorflow -->
    <style name="Theme.Sponsorflow" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Colores primarios -->
        <item name="android:colorPrimary">@android:color/holo_blue_dark</item>
        <item name="android:colorPrimaryDark">@android:color/holo_blue_dark</item>
        <item name="android:colorAccent">@android:color/holo_blue_light</item>
        
        <!-- Color de fondo -->
        <item name="android:colorBackground">@android:color/white</item>
        
        <!-- Color de la barra de estado -->
        <item name="android:statusBarColor">@android:color/holo_blue_dark</item>
        
        <!-- Color de la barra de navegación -->
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>